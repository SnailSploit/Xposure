# X-POSURE v5.0 Dependencies
# Install: pip install -r requirements.txt

aiohttp>=3.9.0
aiodns>=3.0.0
click>=8.0.0
rich>=13.0.0
pyyaml>=6.0.0
esprima>=4.0
jsbeautifier>=1.14.0

# Recursive crawl + enrichment (optional — installed with pip install -e '.[recon]')
anthropic>=0.40.0
shodan>=1.31.0
